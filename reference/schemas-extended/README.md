Reference material for designing kind-discriminated subschemas during the contract rewrite. The canonical `schemas/contract.schema.json` currently models all of these as open-ended fields (the single biggest looseness in the original). During the rewrite, collapse each into a tight required-fields subschema selected by the `kind` discriminator. Do not promote any of these to canonical unless they survive the redesign.
