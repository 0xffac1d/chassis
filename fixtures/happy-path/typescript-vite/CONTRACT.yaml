name: "typescript-vite"
kind: library
version: "0.1.0"
purpose: "TypeScript + Vite happy-path fixture exercising contract validation against a real frontend package layout."
status: stable
since: "0.1.0"
assurance_level: declared
owner: chassis-fixtures
exports:
  - path: "src/main.ts"
    kind: module
invariants:
  - id: typescript-vite.tsc-clean
    text: "tsc --noEmit completes without errors"
  - id: typescript-vite.vite-builds
    text: "vite build produces a dist/ artifact"
edge_cases:
  - id: typescript-vite.no-tests-yet
    text: "Fixture intentionally ships without a test suite to keep scope minimal"
