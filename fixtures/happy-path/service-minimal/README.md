# service-minimal

Minimal happy-path fixture for `kind: service` contracts.

Exercises required `protocol`, `endpoints`, `consumes`, `produces`. Includes an optional `resilience` object to demonstrate the open-shape pass-through.
