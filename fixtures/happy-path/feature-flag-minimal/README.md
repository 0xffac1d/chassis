# feature-flag-minimal

Minimal happy-path fixture for `kind: feature-flag` contracts.

Exercises the type enum (`bool`/`string`/`number`/`json`), `defaultValue`, structured `targeting` (`rules` + `default_variation`), and a non-empty `metrics` array.
