# entity-minimal

Minimal happy-path fixture for `kind: entity` contracts.

Exercises structured `fields` (with nullable/indexed flags) and `relationships` (`one-to-one`, `one-to-many`).
`indexes` and `timestamps` remain optional in Wave 2 and are included here for breadth.
