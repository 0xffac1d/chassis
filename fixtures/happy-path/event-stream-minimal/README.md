# event-stream-minimal

Minimal happy-path fixture for `kind: event-stream` contracts.

Exercises `source`, format-tagged `payload`, the `delivery` enum (`at-least-once`/`at-most-once`/`exactly-once`/`unknown`), and `consumers`.
