name: "rust-minimal"
kind: crate
purpose: "Smallest-possible Rust adoption fixture used to validate the canonical contract schema and the chassis-core CanonicalMetadataContractValidator."
status: stable
since: "0.1.0"
invariants:
  - id: rust-minimal.compiles
    text: "src/main.rs compiles under the host Rust toolchain"
edge_cases:
  - id: rust-minimal.empty-deps
    text: "Cargo.toml has no [dependencies] section"
