# endpoint-minimal

Minimal happy-path fixture for `kind: endpoint` contracts.

Exercises method, path, structured auth object, request/response payloads (with `content_type` + `schema_ref`), and a single request/response example.
