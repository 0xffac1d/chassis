# component-minimal

Minimal happy-path fixture for `kind: component` contracts.

Exercises the four required structured arrays: `props`, `events`, `slots`, `states`.
`accessibility`, `dependencies`, and `ui_taxonomy` remain optional in Wave 2.
